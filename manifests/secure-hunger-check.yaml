apiVersion: v1
kind: ServiceAccount
metadata:
  name: secure-hunger-check
  namespace: security-lab
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: secure-hunger-check
  namespace: security-lab
  labels:
    app: secure-hunger-check
spec:
  replicas: 1
  selector:
    matchLabels:
      app: secure-hunger-check
  template:
    metadata:
      labels:
        app: secure-hunger-check
    spec:
      serviceAccountName: secure-hunger-check
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: hunger-check
          image: madhuakula/k8s-goat-hunger-check
          ports:
            - containerPort: 8080
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: "20m"
              memory: "64Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: secure-hunger-check-default-deny
  namespace: security-lab
spec:
  podSelector:
    matchLabels:
      app: secure-hunger-check
  policyTypes:
    - Ingress
    - Egress